{
  "_comment": "Settings Claude Code — projet ft2e-site. Activer les hooks après scaffolding initial.",
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(npm install)",
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(npx astro *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force*)",
      "Bash(git push -f*)"
    ]
  },
  "_comment_hooks": "Le hook Stop (fin de chaque réponse) construit puis commit+push si l'arbre a changé — build rouge = ni commit ni push ; livrables/ exclu (dépôt public). Script : scripts/hooks/fin-de-reponse.sh, journal : .git/fin-de-reponse.log.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/scripts/hooks/fin-de-reponse.sh\"",
            "shell": "bash",
            "async": true,
            "timeout": 300,
            "statusMessage": "Build + commit + push de fin de réponse…"
          }
        ]
      }
    ]
  },
  "env": {
    "_comment": "Variables d'env à exposer à Claude Code ; ne JAMAIS y mettre de secret."
  }
}
