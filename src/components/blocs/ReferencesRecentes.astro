---
import { getCollection } from 'astro:content';
import CarteProjet from './CarteProjet.astro';
import { parAffaireDecroissante } from '../../lib/projets';

// Section des références récentes — charte v3 : titre de section à puce
// profonde, plans posés (CarteProjet) en grille sur la trame.
interface Props {
  /** Numéro de section sur la planche (ex. « 05 ») — optionnel. */
  numero?: string;
}

const { numero } = Astro.props;

const projets = (await getCollection('projets'))
  .filter((p) => p.data.en_avant)
  .sort(parAffaireDecroissante)
  .slice(0, 4);
---

<section class="py-12 md:py-[76px]">
  <div class="max-w-[1200px] mx-auto px-4 md:px-6">
    <div class="flex items-center gap-3">
      <span class="puce-section" aria-hidden="true"></span>
      <span class="mono-label text-pivot">{numero ? `${numero} — références` : 'références'}</span>
    </div>
    <div class="flex flex-wrap items-baseline justify-between gap-x-6 gap-y-2 mt-4">
      <h2 class="type-section text-[26px]">
        <span class="text-encre">Références récentes</span>
      </h2>
      <a href="/references" class="mono-label font-normal text-pivot hover:text-encre transition-colors duration-[260ms] ease-[var(--ease-blueprint)]">
        voir toutes les références →
      </a>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-8">
      {projets.map((projet) => (
        <CarteProjet projet={projet} />
      ))}
    </div>
  </div>
</section>
